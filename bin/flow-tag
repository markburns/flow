#!/usr/bin/env ruby
# frozen_string_literal: true
#
tag = `flow-tag-next`

if ARGV[0]
  puts "Current highest tag is #{`flow-tag-current`}"
  puts "would tag with #{tag}"
else
  puts "tagging as #{tag}"
  `git tag #{tag}`
  puts 'pushing tags'
  `git push --tags`
end
